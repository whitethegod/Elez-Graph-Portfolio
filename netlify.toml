[build]
  command = "npm run build"
  publish = "dist"

# Disable any auto-detected plugins
[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
